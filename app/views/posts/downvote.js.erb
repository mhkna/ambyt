$(document).ready(function(){
  $('#vote_<%= @post.id %>').html("<%= escape_javascript render :partial => 'vote', locals: { post: @post } %>");
  $('#vote_<%= @post.id %>').find('.glyphicon-chevron-down').addClass('downvote');
});
